import {
  SPA_IMAGES,
  SPA_PACKAGE_META,
  SPA_SERVICE_META,
  SPA_AMENITY_ICONS,
  SPA_TESTIMONIAL_META,
} from '../spa/spa-data';

// ----------------------------------------------------------------------

// `spa1` is a styling variant of the spa landing page. It reuses the same static
// metadata (images, icons, prices, avatars); only the visual treatment differs.

export { SPA_IMAGES, SPA_PACKAGE_META, SPA_SERVICE_META, SPA_AMENITY_ICONS, SPA_TESTIMONIAL_META };

// Luxury dark palette tokens used across the spa1 sections.
export const SPA1_DARK = '#0E0E10';
export const SPA1_DARK_ALT = '#16161A';
export const SPA1_GOLD = '#C8A15A';
export const SPA1_GOLD_SOFT = '#E3C988';
